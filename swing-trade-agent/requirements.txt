# Core (trust-critical modules run on just these)
pandas>=2.0
numpy>=1.24
requests>=2.31
python-dotenv>=1.0

# Indicators — deterministic technical-analysis maths (numba accelerates it)
pandas-ta-classic>=0.6
numba>=0.60

# Price data
yfinance>=0.2.40
pyarrow>=14.0

# Reasoning (Claude API)
anthropic>=0.69

# Output persistence
supabase>=2.4

# Dev / test
pytest>=7.4
pytest-cov>=4.1

